///@func show_scaled_string(text, width, font)
///@param text
///@param width
///@param size
var arg = 0;
var text = argument[arg++];
var width = argument[arg++];
var size = argument_count >= arg ? argument[arg++] : 1;

var scale = 