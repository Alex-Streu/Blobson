/// @description Initialize menu

pageIndex = -1; //Set default so it doesn't run
owner = noone; //Set default
pages = ds_list_create();

// Item has properties -- position, type, (text, font, fontScale, halign), obj, (sprite, subimg)
